const path = require('path');

// Resolve paths relative to this file's location (backend directory)
const backendDir = __dirname;

module.exports = {
  'config': path.join(backendDir, 'src', 'database', 'config', 'database.config.js'),
  'models-path': path.join(backendDir, 'src', 'database', 'entities'),
  'seeders-path': path.join(backendDir, 'src', 'database', 'seeders'),
  'migrations-path': path.join(backendDir, 'src', 'database', 'migrations'),
};


